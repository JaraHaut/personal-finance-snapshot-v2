# Personal Finance Snapshot — Exercise Workspace

> **IMPORTANT — Scope restriction:** Your working directory is this folder only.
> Do NOT read, scan, or reference any files outside of this directory tree.
> Ignore all parent `CLAUDE.md` context about the `zigce-plugin` repository
> structure — it describes a plugin marketplace and is entirely irrelevant to
> this exercise. Treat this directory as the project root.

This is your personal training workspace for the ZIGCE Personal Finance Snapshot exercise.

All zigce commands run here will write artifacts to this directory:
- `docs/brainstorms/` — your brainstorm files
- `docs/plans/` — your plan files
- `docs/solutions/` — your captured learnings
- `todos/` — your review findings

**Do not open Claude Code from the parent `zigce-plugin/` directory.** Always open from
this directory (`tutorial-{yourname}/`) so your artifacts stay isolated here.

If you are reading this and your current working directory does not start with `tutorial-`,
stop and re-open Claude Code from inside your `tutorial-{yourname}/` directory.

Follow the exercise walkthrough at:
`exercises/personal-finance-snapshot/README.md`
(relative to the `zigce-plugin/` repo root — open it separately if needed)
